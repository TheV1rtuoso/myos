Add highlevel paging support, Add translation functions for pt
unmap initialized page tables setup memeory management
TODO serial printf
smart pointer impl -> VGA Memory Access (stdout wrapper)
!! printlibrary
!! libc setup
! cleanup and improve cmake setup / fix iso
assert
nicer abort: libc to see if kernel or userspace aborted: abort()
cpuinfo
Add keyboard driver
Add blinking cursor
high level interrupts
Add funny pseudo userspace program
Add malloc / automatic pt allocation
push segments for priv change in interrupts


PhysicalSpace -> get_phy_pages(size_t)
VirtualSpace -> map_pages(phy_page*)

init_heap -> WaterMark(get_phypages) Stub (for free)
